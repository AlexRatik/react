export interface ICardProps {
  house: ICard;
}

export interface ICard {
  id: string;
  name: string;
  photoURL: string;
  price: string;
  square: string;
  height: string;
  costOfConstruction: string;
}
